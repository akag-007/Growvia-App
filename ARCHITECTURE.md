# Architecture & Development Strategy

**Project**: Growvia (Configurable)
**Philosophy**: Startup-grade, Scalable, Secure, Production-Ready.
**Status**: Pre-Alpha (Phase 1 Initialization)

---

## 🏗️ Monorepo Structure

We follow a standard monorepo structure to share business logic between Web and Mobile.

- **`apps/web`**: Next.js (App Router) - The main dashboard and web interface.
- **`apps/mobile`**: React Native (Expo) - The mobile application.
- **`packages/shared`**: Shared business logic, constants, types, and state management (Zustand stores).

## 🛠️ Technology Stack (Locked)

- **Frontend**: Next.js, TypeScript, Tailwind CSS
- **Mobile**: React Native (Expo)
- **Backend Service**: Supabase (Auth, DB, Realtime, Storage)
- **State Management**: Zustand (Client), TanStack Query (Server State)
- **Database**: PostgreSQL (via Supabase)

## 📐 Core Architecture Principles

1.  **Incremental Build**: We build feature-by-feature. No "big bang" releases.
2.  **Offline-First**: The app must work offline. We use an "Additive Log" strategy for time tracking to prevent data loss.
3.  **Security**: OWASP best practices. RLS policies are mandatory.
4.  **Aesthetics**: Premium, modern, "wow" factor.

## 📅 Roadmap Protocols

### PHASE 1: Authentication & Base System
- [ ] Initialize Repository Structure
- [ ] Setup Supabase Project (External)
- [ ] Implement Offline Queue Engine (`packages/shared`)
- [ ] Basic Auth Flows (Web/Mobile)

### PHASE 2: Daily Tasks
- [ ] Task CRUD
- [ ] Timer Logic (Event-based)

*(See full roadmap in Project Spec)*

## 🔒 Security Mandates
- **Input Sanitization**: All inputs must be validated (Zod).
- **RLS**: Row Level Security enabled on ALL tables.
- **No Client Secrets**: Environment variables only.

## 🎨 UI/UX Guidelines
- **Theme**: Configurable via `brand.config.ts`.
- **Mode**: Dark/Light mode support required.
- **Motion**: Use `framer-motion` (Web) and `react-native-reanimated` (Mobile).

---
*Generated by Antigravity Agent based on User Requirements.*
